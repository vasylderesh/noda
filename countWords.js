/**
 * Given an input string, returns number of words contained in the string.
 *
 * In our xase, assume words are always separated by a single space.
 *
 * @param {string} string - The input string
 * @returns {number} The number of words in the string.
 */

function countWords(string) {
    // Put your code here
}
  
 
console.log('Running sanity checks for countWords:');

console.log(countWords('Running sanity checks for countWords') == 5);

// Add your own sanity checks here.
